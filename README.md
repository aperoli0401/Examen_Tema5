# Examen_Tema5

## Preguntas teoricas

2. La clase biblioteca debe ser abstracta para que así pueda recojer los valores y datos que le pasen sus hijos, de esta manera conseguimos que de un metodo podamos obtener distintos resultados.

3. Porque "libro" no es el padre de autor.

4. De esta manera podemos hacer que las clases esten diferenciadas y se mantengan indiferentemente.

5. Para proteger que el resto de las clases.

6. Si el contador no fuera estatico cada objeto tendría su propio contador y se resetearía al empezar siempre.

7. La herencia se usa mas para los "es un" en jerarquias, mientras que la composicion se usa mas en las "tiene un" en funcionalidades compartidas.

8. toString(), equals() y hashcode().

9. Esta es la capacidad de tratar los objetos de diferentes clases (Libro, resvista) de forma uniforme.

10. 
10.1 Imprime "java"
10.2 Porque en java los strings son inmutables y los argumentos se pasan por valor. El metodo cambia la referencia no el objeto.